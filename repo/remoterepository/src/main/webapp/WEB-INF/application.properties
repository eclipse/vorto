server.port = 8080

vorto.repo.dir = ${user.home}/.vorto/repo
vorto.index.dir = ${user.home}/.vorto/lucene/

#If 'vorto.rebuild.index' value set to TRUE, the lucene index directory will be cleaned (folder deleted and re-created) and 
#all model files will be re-indexed during server bootup. The process will be difference from REST API for deleteAllIndex, where
#lucene will just remove all index documents by itself.
vorto.rebuild.index = false
